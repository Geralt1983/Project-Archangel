# Example Supabase environment (do not commit real credentials)
# Copy to .env or export in your shell before running make test.supabase

# Supabase connection string; ensure sslmode=require
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Optional Redis if used
# REDIS_URL=redis://localhost:6379/0

